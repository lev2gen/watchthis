# Copy to deploy/.env and fill in real values. Never commit .env.

# Password for the postgres container (any long random string)
POSTGRES_PASSWORD=change-me

# Public site URL
SITE_URL=https://watchthis.dev
